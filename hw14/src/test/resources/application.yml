spring:
  datasource:
    url: jdbc:h2:mem:testdb
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-test.yaml
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true